# ⚡ VERCEL + SUPABASE: ШВИДКИЙ СТАРТ (5 ХВИЛИН)

> 💡 Якщо вам показує помилку "Supabase configuration required", виконайте цей гайд.

## 🎯 5 КРОКІВ ДО ГОТОВОГО САЙТУ

### КРОК 1: Supabase (2 хвилини)
```
1️⃣  Перейти: https://supabase.com → "Start your project"
2️⃣  Заповнити: Назва = "mikola-clinic", Пароль = будь-який, Plan = Free
3️⃣  Чекати: ~2 хвилини на готовність
4️⃣  Копіювати: Settings → API → Project URL + anon key
```

### КРОК 2: Vercel (1 хвилина)
```
1️⃣  Перейти: https://vercel.com/dashboard
2️⃣  Проект: mikola-nov-capi-1708 → Settings → Environment Variables
3️⃣  Додати змінні:
    - NEXT_PUBLIC_SUPABASE_URL = https://xxxxx.supabase.co
    - NEXT_PUBLIC_SUPABASE_ANON_KEY = eyJ0eXAi...
4️⃣  Production + Preview ✅
```

### КРОК 3: База даних (1 хвилина)
```
1️⃣  Supabase: SQL Editor → New Query
2️⃣  Скопіювати код нижче ⬇️
3️⃣  Натиснути: RUN ▶️
```

**Скопіюйте і вставте цей код:**
```sql
CREATE TABLE IF NOT EXISTS medical_forms (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NOT NULL, phone TEXT NOT NULL, age TEXT, gender TEXT,
  height DECIMAL(5,2), weight DECIMAL(5,2), bmi DECIMAL(4,1),
  complaints TEXT, examinations TEXT[] DEFAULT '{}',
  has_chronic_disease BOOLEAN DEFAULT false, chronic_diseases TEXT,
  takes_medication BOOLEAN DEFAULT false, medications TEXT,
  pain_level INTEGER DEFAULT 0, additional_comments TEXT,
  user_agent TEXT, is_mobile BOOLEAN DEFAULT false,
  viewport TEXT, connection TEXT, platform TEXT,
  cookie_enabled BOOLEAN DEFAULT false, status TEXT DEFAULT 'pending',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

CREATE TABLE IF NOT EXISTS consultations (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NOT NULL, phone TEXT NOT NULL, comment TEXT, date TEXT,
  user_agent TEXT, is_mobile BOOLEAN DEFAULT false, viewport TEXT, platform TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

ALTER TABLE medical_forms ENABLE ROW LEVEL SECURITY;
ALTER TABLE consultations ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow all" ON medical_forms FOR ALL USING (true);
CREATE POLICY "Allow all" ON consultations FOR ALL USING (true);
```

### КРОК 4: Редеплой (1 хвилина)
```
1️⃣  Vercel: Deployments → Натиснути останній deployment
2️⃣  Кнопка: Redeploy
3️⃣  Чекати: 2-3 хвилини
```

### КРОК 5: Тест ✅
```
1️⃣  Відкрити: ваш-домен.vercel.app/medical-form
2️⃣  Заповнити форму та натиснути: Відправити
3️⃣  Перевірити: Supabase → Table Editor → medical_forms
4️⃣  Готово! ✅
```

---

## ❓ ЧАСТІ ПОМИЛКИ

| Помилка | Рішення |
|---------|---------|
| `Invalid API key` | Скопіювали **anon key** (не service_role)? |
| `Connection failed` | URL правильний? `https://xxxxx.supabase.co` |
| `Дані не зберігаються` | SQL код виконаний? RLS включений? |
| `Vercel не розпізнає змінні` | Додали в Production + Preview? |

---

## 📚 БІЛЬШЕ ІНФОРМАЦІЇ

- **Детальний гайд**: [VERCEL_SUPABASE_SETUP.md](./VERCEL_SUPABASE_SETUP.md)
- **Налаштування локально**: [SUPABASE_SETUP.md](./SUPABASE_SETUP.md)
- **Диагностика**: Логи → Vercel Dashboard → Deployments → Logs

---

**Версія**: 2.0  
**Дата**: 27 грудня 2025  
**Статус**: ✅ ГОТОВО
